<Window x:Class="ProProSahur.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pro Pro Sahur"
        Width="350" Height="280"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        >
    <Grid>
        <Border x:Name="SpeechBubble" Visibility="Collapsed" Background="#FFFDE7" CornerRadius="12"
                Padding="12" BorderBrush="#333" BorderThickness="1" MaxWidth="320" MaxHeight="220"
                HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,20,135">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="2" Opacity="0.3"/>
            </Border.Effect>
            <TextBlock x:Name="SpeechText" TextWrapping="Wrap" FontSize="13" Foreground="#333"/>
        </Border>
        <Border x:Name="CharacterCanvas" Width="120" Height="120" HorizontalAlignment="Right"
                VerticalAlignment="Bottom" Margin="0,0,10,0" Background="Transparent">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="2" Opacity="0.5"/>
            </Border.Effect>
            <Image x:Name="CharacterImage" Source="pack://application:,,,/ProProSahur;component/Assets/tung-tung-sahur.png" Stretch="Uniform"
                   RenderTransformOrigin="0.5,0.5"/>
        </Border>
    </Grid>
</Window>
